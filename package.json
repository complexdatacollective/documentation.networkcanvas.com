{
  "name": "documentation-site",
  "license": "GPL",
  "private": true,
  "engines": {
    "node": "12.14.1",
    "npm": "6.13.4"
},
  "devDependencies": {
    "markdown-spellcheck": "^1.3.1"
  },
  "scripts": {
    "spellcheck": "mdspell --en-us --ignore-numbers --ignore-acronyms --no-suggestions --report '**/*.md' '!**/node_modules/**/*.md'",
    "proselint": "ls ./**/*.md | grep -v node_modules | xargs proselint"
  }
}
